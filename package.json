{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.0",
  "description": "{{PROJECT_DESCRIPTION}}",
  "main": "index.js",
  "scripts": {
    "commit": "npx git-cz",
    "dev": "echo \"Error: no dev command specified\" && exit 1",
    "start": "echo \"Error: no start command specified\" && exit 1",
    "test": "echo \"Error: no test command specified\" && exit 1",
    "lint": "echo \"Error: no lint command specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+{{REPO_URL}}"
  },
  "author": "{{AUTHOR_NAME}} <{{AUTHOR_EMAIL}}>",
  "license": "MIT",
  "bugs": {
    "url": "{{REPO_URL}}/issues"
  },
  "homepage": "{{REPO_URL}}#readme",
  "devDependencies": {
    "cz-conventional-changelog": "3.2.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
